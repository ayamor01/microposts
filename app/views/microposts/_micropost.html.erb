<li id="micropost-<%= micropost.id %>" class="clearfix">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
  </span>
  <span class="favorite_btn">
    <% if current_user.favorited_past?(micropost) %>
      <%= render partial: 'microposts/unfavorite', locals: {micropost: micropost} if logged_in? %>
    <% else %>
      <%= render partial: 'microposts/favorite', locals: {micropost: micropost} if logged_in?  %>
    <% end %>
  </span>
</li>