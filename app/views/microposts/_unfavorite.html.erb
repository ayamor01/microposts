<%= form_for(current_user.favorite_relationships.find_by(favorited_id: micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
        <%= f.submit "Unfavorite", class: "btn col-xs-2" %>
<% end %>